diff --git a/dist/index.cjs b/dist/index.cjs
index 4709730f1d6608dee4ffb3a386d1732f776c5f5c..fbea052f5bf4595bac907392f9f75bafefc28588 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -202,9 +202,8 @@ function getDpr() {
   if (typeof window === "undefined") return 1;
   return Math.min(window.devicePixelRatio || 1, 3);
 }
-function applyDpr(ctx, dpr, w, h) {
+function applyDpr(ctx, dpr, _w, _h) {
   ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
-  ctx.clearRect(0, 0, w, h);
 }
 
 // src/draw/grid.ts
@@ -256,7 +255,7 @@ function drawGrid(ctx, layout, palette, formatValue, state, dt) {
     if (y < pad.top - 2 || y > h - pad.bottom + 2) continue;
     const isCoarse = divisible(val, coarse);
     const target = (isCoarse ? 1 : fineTarget) * edgeAlpha(y);
-    const key = Math.round(val * 1e3);
+    const key = Math.round(val * 1e6);
     targets.set(key, target);
   }
   for (const [key, alpha] of state.labels) {
@@ -282,7 +281,7 @@ function drawGrid(ctx, layout, palette, formatValue, state, dt) {
   ctx.textAlign = "left";
   for (const [key, alpha] of state.labels) {
     if (alpha < 0.02) continue;
-    const val = key / 1e3;
+    const val = key / 1e6;
     const y = toY(val);
     if (y < pad.top - 10 || y > h - pad.bottom + 10) continue;
     ctx.save();
@@ -1945,27 +1944,22 @@ function drawLoading(ctx, w, h, pad, palette, now_ms, alpha = 1, strokeColor) {
 
 // src/draw/badge.ts
 function badgeSvgPath(pillW, pillH, tailLen, tailSpread) {
-  const r = pillH / 2;
-  const cx = tailLen + pillW - r;
-  const tl = tailLen + r;
+  const mid = pillH / 2;
   return [
-    `M${tl},0`,
-    `L${cx},0`,
-    `A${r},${r},0,0,1,${cx},${pillH}`,
-    `L${tl},${pillH}`,
-    `C${tailLen + 2},${pillH},${3},${r + tailSpread},0,${r}`,
-    `C${3},${r - tailSpread},${tailLen + 2},0,${tl},0`,
+    `M${tailLen},0`,
+    `L${tailLen + pillW},0`,
+    `L${tailLen + pillW},${pillH}`,
+    `L${tailLen},${pillH}`,
+    `L0,${mid}`,
     "Z"
   ].join(" ");
 }
 function badgePillOnly(pillW, pillH) {
-  const r = pillH / 2;
   return [
-    `M${r},0`,
-    `L${pillW - r},0`,
-    `A${r},${r},0,0,1,${pillW - r},${pillH}`,
-    `L${r},${pillH}`,
-    `A${r},${r},0,0,1,${r},0`,
+    `M0,0`,
+    `L${pillW},0`,
+    `L${pillW},${pillH}`,
+    `L0,${pillH}`,
     "Z"
   ].join(" ");
 }
@@ -2178,7 +2172,7 @@ function updateBadgeDOM(badge, cfg, smoothValue, layout, momentum, badgeY, badge
     const badgeSpeed = isWindowTransitioning ? BADGE_Y_LERP_TRANSITIONING : BADGE_Y_LERP;
     badgeY = lerp(badgeY, targetBadgeY, badgeSpeed, dt);
   }
-  const badgeLeft = w - pad.right + 8 - BADGE_PAD_X - tailLen;
+  const badgeLeft = w - pad.right - BADGE_PAD_X - tailLen;
   const badgeTop = badgeY - pillH / 2;
   badge.container.style.transform = `translate3d(${badgeLeft}px, ${badgeTop}px, 0)`;
   if (cfg.badgeVariant === "minimal") {
@@ -2484,17 +2478,32 @@ function useLivelineEngine(canvasRef, containerRef, config) {
     mql.addEventListener("change", onChange);
     return () => mql.removeEventListener("change", onChange);
   }, []);
+  const visibleRef = (0, import_react.useRef)(true);
   (0, import_react.useEffect)(() => {
     const onVisibility = () => {
-      if (!document.hidden && !rafRef.current) {
+      if (!document.hidden && visibleRef.current && !rafRef.current) {
         rafRef.current = requestAnimationFrame(draw);
       }
     };
     document.addEventListener("visibilitychange", onVisibility);
     return () => document.removeEventListener("visibilitychange", onVisibility);
   }, []);
+  (0, import_react.useEffect)(() => {
+    const container = containerRef.current;
+    if (!container) return;
+    const io = new IntersectionObserver((entries) => {
+      const entry = entries[0];
+      if (!entry) return;
+      visibleRef.current = entry.isIntersecting;
+      if (entry.isIntersecting && !document.hidden && !rafRef.current) {
+        rafRef.current = requestAnimationFrame(draw);
+      }
+    }, { rootMargin: "200px" });
+    io.observe(container);
+    return () => io.disconnect();
+  }, [containerRef]);
   const draw = (0, import_react.useCallback)(() => {
-    if (document.hidden) {
+    if (document.hidden || !visibleRef.current) {
       rafRef.current = 0;
       return;
     }
@@ -2639,6 +2648,7 @@ function useLivelineEngine(canvasRef, containerRef, config) {
       }
     }
     if (!hasData && !useStash && !useMultiStash) {
+      ctx.clearRect(0, 0, w, h);
       const loadingColor = isCandle || cfg.isMultiSeries || lastMultiSeriesRef.current.length > 0 ? cfg.palette.gridLabel : void 0;
       if (loadingAlpha > 0.01) {
         drawLoading(ctx, w, h, pad, cfg.palette, now_ms, loadingAlpha, loadingColor);
@@ -2659,6 +2669,7 @@ function useLivelineEngine(canvasRef, containerRef, config) {
       return;
     }
     if (isCandle) {
+      ctx.clearRect(0, 0, w, h);
       const candleBuffer = CANDLE_BUFFER_NO_BADGE;
       if (hasData) frozenNowRef.current = Date.now() / 1e3 - timeDebtRef.current;
       const now = hasData || chartReveal < 5e-3 ? Date.now() / 1e3 - timeDebtRef.current : frozenNowRef.current;
@@ -3064,6 +3075,7 @@ function useLivelineEngine(canvasRef, containerRef, config) {
         }
       }
     } else if (cfg.isMultiSeries && cfg.multiSeries && cfg.multiSeries.length > 0 || useMultiStash) {
+      ctx.clearRect(0, 0, w, h);
       const effectiveMultiSeries = useMultiStash ? lastMultiSeriesRef.current : cfg.multiSeries;
       let labelReserve = 0;
       if (effectiveMultiSeries.some((s) => s.label)) {
@@ -3330,7 +3342,7 @@ function useLivelineEngine(canvasRef, containerRef, config) {
       const chartW = w - pad.left - pad.right;
       const baseBuffer = cfg.showBadge ? WINDOW_BUFFER : WINDOW_BUFFER_NO_BADGE;
       const needsArrowRoom = cfg.showMomentum && cfg.showBadge;
-      const buffer = needsArrowRoom ? Math.max(baseBuffer, 37 / Math.max(chartW, 1)) : baseBuffer;
+      const buffer = needsArrowRoom ? Math.max(baseBuffer, 52 / Math.max(chartW, 1)) : baseBuffer;
       const transition = windowTransitionRef.current;
       if (hasData) frozenNowRef.current = Date.now() / 1e3 - timeDebtRef.current;
       const now = useStash ? frozenNowRef.current : Date.now() / 1e3 - timeDebtRef.current;
@@ -3364,6 +3376,7 @@ function useLivelineEngine(canvasRef, containerRef, config) {
         rafRef.current = requestAnimationFrame(draw);
         return;
       }
+      ctx.clearRect(0, 0, w, h);
       const computedRange = computeRange(visible, smoothValue, cfg.referenceLine?.value, cfg.exaggerate);
       const isWindowTransitioning = transition.startMs > 0;
       const rangeResult = updateRange(
diff --git a/dist/index.js b/dist/index.js
index fd0506272e9a54432a0882c3a389ee74302e4718..33674e1379bc8bf60e2e7fd379d880f019ee95dd 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -175,9 +175,8 @@ function getDpr() {
   if (typeof window === "undefined") return 1;
   return Math.min(window.devicePixelRatio || 1, 3);
 }
-function applyDpr(ctx, dpr, w, h) {
+function applyDpr(ctx, dpr, _w, _h) {
   ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
-  ctx.clearRect(0, 0, w, h);
 }
 
 // src/draw/grid.ts
@@ -229,7 +228,7 @@ function drawGrid(ctx, layout, palette, formatValue, state, dt) {
     if (y < pad.top - 2 || y > h - pad.bottom + 2) continue;
     const isCoarse = divisible(val, coarse);
     const target = (isCoarse ? 1 : fineTarget) * edgeAlpha(y);
-    const key = Math.round(val * 1e3);
+    const key = Math.round(val * 1e6);
     targets.set(key, target);
   }
   for (const [key, alpha] of state.labels) {
@@ -255,7 +254,7 @@ function drawGrid(ctx, layout, palette, formatValue, state, dt) {
   ctx.textAlign = "left";
   for (const [key, alpha] of state.labels) {
     if (alpha < 0.02) continue;
-    const val = key / 1e3;
+    const val = key / 1e6;
     const y = toY(val);
     if (y < pad.top - 10 || y > h - pad.bottom + 10) continue;
     ctx.save();
@@ -1918,27 +1917,22 @@ function drawLoading(ctx, w, h, pad, palette, now_ms, alpha = 1, strokeColor) {
 
 // src/draw/badge.ts
 function badgeSvgPath(pillW, pillH, tailLen, tailSpread) {
-  const r = pillH / 2;
-  const cx = tailLen + pillW - r;
-  const tl = tailLen + r;
+  const mid = pillH / 2;
   return [
-    `M${tl},0`,
-    `L${cx},0`,
-    `A${r},${r},0,0,1,${cx},${pillH}`,
-    `L${tl},${pillH}`,
-    `C${tailLen + 2},${pillH},${3},${r + tailSpread},0,${r}`,
-    `C${3},${r - tailSpread},${tailLen + 2},0,${tl},0`,
+    `M${tailLen},0`,
+    `L${tailLen + pillW},0`,
+    `L${tailLen + pillW},${pillH}`,
+    `L${tailLen},${pillH}`,
+    `L0,${mid}`,
     "Z"
   ].join(" ");
 }
 function badgePillOnly(pillW, pillH) {
-  const r = pillH / 2;
   return [
-    `M${r},0`,
-    `L${pillW - r},0`,
-    `A${r},${r},0,0,1,${pillW - r},${pillH}`,
-    `L${r},${pillH}`,
-    `A${r},${r},0,0,1,${r},0`,
+    `M0,0`,
+    `L${pillW},0`,
+    `L${pillW},${pillH}`,
+    `L0,${pillH}`,
     "Z"
   ].join(" ");
 }
@@ -2151,7 +2145,7 @@ function updateBadgeDOM(badge, cfg, smoothValue, layout, momentum, badgeY, badge
     const badgeSpeed = isWindowTransitioning ? BADGE_Y_LERP_TRANSITIONING : BADGE_Y_LERP;
     badgeY = lerp(badgeY, targetBadgeY, badgeSpeed, dt);
   }
-  const badgeLeft = w - pad.right + 8 - BADGE_PAD_X - tailLen;
+  const badgeLeft = w - pad.right - BADGE_PAD_X - tailLen;
   const badgeTop = badgeY - pillH / 2;
   badge.container.style.transform = `translate3d(${badgeLeft}px, ${badgeTop}px, 0)`;
   if (cfg.badgeVariant === "minimal") {
@@ -2457,17 +2451,32 @@ function useLivelineEngine(canvasRef, containerRef, config) {
     mql.addEventListener("change", onChange);
     return () => mql.removeEventListener("change", onChange);
   }, []);
+  const visibleRef = useRef(true);
   useEffect(() => {
     const onVisibility = () => {
-      if (!document.hidden && !rafRef.current) {
+      if (!document.hidden && visibleRef.current && !rafRef.current) {
         rafRef.current = requestAnimationFrame(draw);
       }
     };
     document.addEventListener("visibilitychange", onVisibility);
     return () => document.removeEventListener("visibilitychange", onVisibility);
   }, []);
+  useEffect(() => {
+    const container = containerRef.current;
+    if (!container) return;
+    const io = new IntersectionObserver((entries) => {
+      const entry = entries[0];
+      if (!entry) return;
+      visibleRef.current = entry.isIntersecting;
+      if (entry.isIntersecting && !document.hidden && !rafRef.current) {
+        rafRef.current = requestAnimationFrame(draw);
+      }
+    }, { rootMargin: "200px" });
+    io.observe(container);
+    return () => io.disconnect();
+  }, [containerRef]);
   const draw = useCallback(() => {
-    if (document.hidden) {
+    if (document.hidden || !visibleRef.current) {
       rafRef.current = 0;
       return;
     }
@@ -2612,6 +2621,7 @@ function useLivelineEngine(canvasRef, containerRef, config) {
       }
     }
     if (!hasData && !useStash && !useMultiStash) {
+      ctx.clearRect(0, 0, w, h);
       const loadingColor = isCandle || cfg.isMultiSeries || lastMultiSeriesRef.current.length > 0 ? cfg.palette.gridLabel : void 0;
       if (loadingAlpha > 0.01) {
         drawLoading(ctx, w, h, pad, cfg.palette, now_ms, loadingAlpha, loadingColor);
@@ -2632,6 +2642,7 @@ function useLivelineEngine(canvasRef, containerRef, config) {
       return;
     }
     if (isCandle) {
+      ctx.clearRect(0, 0, w, h);
       const candleBuffer = CANDLE_BUFFER_NO_BADGE;
       if (hasData) frozenNowRef.current = Date.now() / 1e3 - timeDebtRef.current;
       const now = hasData || chartReveal < 5e-3 ? Date.now() / 1e3 - timeDebtRef.current : frozenNowRef.current;
@@ -3037,6 +3048,7 @@ function useLivelineEngine(canvasRef, containerRef, config) {
         }
       }
     } else if (cfg.isMultiSeries && cfg.multiSeries && cfg.multiSeries.length > 0 || useMultiStash) {
+      ctx.clearRect(0, 0, w, h);
       const effectiveMultiSeries = useMultiStash ? lastMultiSeriesRef.current : cfg.multiSeries;
       let labelReserve = 0;
       if (effectiveMultiSeries.some((s) => s.label)) {
@@ -3303,7 +3315,7 @@ function useLivelineEngine(canvasRef, containerRef, config) {
       const chartW = w - pad.left - pad.right;
       const baseBuffer = cfg.showBadge ? WINDOW_BUFFER : WINDOW_BUFFER_NO_BADGE;
       const needsArrowRoom = cfg.showMomentum && cfg.showBadge;
-      const buffer = needsArrowRoom ? Math.max(baseBuffer, 37 / Math.max(chartW, 1)) : baseBuffer;
+      const buffer = needsArrowRoom ? Math.max(baseBuffer, 52 / Math.max(chartW, 1)) : baseBuffer;
       const transition = windowTransitionRef.current;
       if (hasData) frozenNowRef.current = Date.now() / 1e3 - timeDebtRef.current;
       const now = useStash ? frozenNowRef.current : Date.now() / 1e3 - timeDebtRef.current;
@@ -3337,6 +3349,7 @@ function useLivelineEngine(canvasRef, containerRef, config) {
         rafRef.current = requestAnimationFrame(draw);
         return;
       }
+      ctx.clearRect(0, 0, w, h);
       const computedRange = computeRange(visible, smoothValue, cfg.referenceLine?.value, cfg.exaggerate);
       const isWindowTransitioning = transition.startMs > 0;
       const rangeResult = updateRange(
