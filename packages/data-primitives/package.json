{
  "name": "@zframes/data-primitives",
  "version": "0.0.0",
  "description": "zframes provider transport primitives: the shared fetchJson/fetchText transport (UA, proxy rewrite, abort, optional Zod validation) and the TtlCache (TTL + in-flight dedup + stale-on-error + opt-in persist). React-free.",
  "license": "Apache-2.0",
  "author": "Zentry",
  "repository": {
    "type": "git",
    "url": "https://github.com/zentryhq/zframes.git",
    "directory": "packages/data-primitives"
  },
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./fetch": "./src/fetch.ts",
    "./cache": "./src/cache.ts"
  },
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@zframes/spec": "workspace:*",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
