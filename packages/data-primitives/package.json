{
  "name": "@zframes/data-primitives",
  "version": "0.0.0",
  "description": "zframes provider transport primitives: the shared fetchJson/fetchText transport (UA, proxy rewrite, abort, optional Zod validation), the TtlCache (TTL + in-flight dedup + stale-on-error + opt-in persist), and quote-aware CSV row parsing for the CSV-published official sources. React-free.",
  "license": "MIT",
  "author": "Zentry",
  "repository": {
    "type": "git",
    "url": "https://github.com/zentryhq/zframes.git",
    "directory": "packages/data-primitives"
  },
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./fetch": "./src/fetch.ts",
    "./cache": "./src/cache.ts",
    "./csv": "./src/csv.ts"
  },
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@zframes/spec": "workspace:*",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
