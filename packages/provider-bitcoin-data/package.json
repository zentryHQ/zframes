{
  "name": "@zframes/provider-bitcoin-data",
  "version": "0.0.0",
  "description": "Free, no-API-key zframes data provider for bitcoin-data.com on-chain cycle oscillators — SOPR, Puell Multiple, Reserve Risk (onchain-cycle-extras capability). Free tier is 10 req/hour, so all metrics load behind one shared long-TTL poll.",
  "license": "Apache-2.0",
  "author": "Zentry",
  "repository": {
    "type": "git",
    "url": "https://github.com/zentryhq/zframes.git",
    "directory": "packages/provider-bitcoin-data"
  },
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@zframes/data-primitives": "workspace:*",
    "@zframes/spec": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
