{
  "name": "@zframes/unicorn",
  "version": "0.0.0",
  "description": "Shared Unicorn Studio scene loader for zframes hosts: injects the self-hosted engine once, mounts/destroys scenes, and exposes the low-end-device / reduced-motion gates hosts use to skip the WebGL backdrop.",
  "license": "Apache-2.0",
  "author": "Zentry",
  "repository": {
    "type": "git",
    "url": "https://github.com/zentryhq/zframes.git",
    "directory": "packages/unicorn"
  },
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./scene": "./src/scene.tsx"
  },
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "react": ">=18"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "typescript": "^7.0.2"
  }
}
