# Next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# deps (also covered by the root .gitignore)
/node_modules

# env + local dev database (PGlite)
.env*
!.env.example
/.pglite

# vendored on predev/prebuild from apps/runtime/public (single source of truth)
/public/widget-icons/
