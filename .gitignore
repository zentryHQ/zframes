node_modules/
dist/
.vite/
*.log
*.tgz
.DS_Store
.env
.env.*

# Internal Claude Design-sync tooling — local-only, never in the public repo.
.ds-sync/
ds-bundle/
.design-sync/
packages/catalogue/

# Derived: the prebuilt browser runtime vendored into the CLI by build:cli
# (a vite build of apps/playground that `zframes serve` ships)
packages/cli/runtime/

# Local-only docs — kept on disk, never committed to the public repo:
# HTML working artifacts (plans, reports, audits) and the decision logs.
# Only docs/assets/ (README images) stays tracked.
docs/*.html
docs/decisions/
