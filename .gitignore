node_modules/
dist/
.vite/
*.log
*.tgz
.DS_Store
.env
.env.*

# design-sync: staged converter scripts + regenerated build output + machine state
# (durable inputs under .design-sync/ — config.json, NOTES.md, conventions.md,
#  previews/, overrides/, ds-entry.tsx — ARE committed)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Derived: the prebuilt browser runtime vendored into the CLI by build:cli
# (a vite build of apps/playground that `zframes serve` ships)
packages/cli/runtime/

# Working-session docs — kept locally, excluded from the public repo.
# (ADRs under docs/decisions/, docs/assets/, and the CENTRAL_PLAN_* / daily-brief
#  docs stay tracked.)
docs/code-review-2026-06-16.html
docs/code-review-2026-06-17.html
docs/session-summary-2026-06-12.html
docs/hyperframes-explainer.html
docs/zframes-architecture.html
docs/zframes-structure.html
docs/open-source-readiness.html
